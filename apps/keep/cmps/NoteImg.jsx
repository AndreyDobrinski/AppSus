export function NoteImg({ info}) {
    return <label>
        {info.txt}
        {/* <input type="range" max={info.max} onChange={(ev) => {
            onAns(+ev.target.value)
        }} /> */}
    </label>
}